<nav class="navbar navbar-expand-lg navbar-custom">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">LingoStep</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Language courses
          </a>
          <ul class="dropdown-menu">
            {% for languageCourse in get_language_courses() %}
            <li><a class="dropdown-item" href="{{ path('language_courses', {'id':languageCourse.id})}}">{{
                languageCourse.language.label
                }}</a>
            </li>
            {% endfor %}
            <li>
              <hr class="dropdown-divider">
            </li>
            <li><a class="dropdown-item" href="{{ path('language_courses_list')}}">All language courses</a></li>
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" aria-disabled="true" href="#"><span class="text-body-tertiary">Ranking</span></a>
        </li>
        {% if app.user %}
        <li>
          <ul class="navbar-nav me-3 mb-2 mb-lg-0">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                aria-expanded="false">Profile</a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('app_user_profile') }}">View Profile</a></li>
                <li><a class="dropdown-item" href="{{ path('app_user_profile_edit') }}">Edit Profile</a></li>
                <li><a class="dropdown-item" href="{{ path('user_answered_questions') }}">My missions</a></li>
                <li><a class="dropdown-item disabled" aria-disabled="true" href="#">Settings</a></li>
              </ul>
            </li>
          </ul>
        </li>
        <li>
          <ul class="navbar-nav me-3 mb-2 mb-lg-0">
            <li class="nav-item text-align text-white">{{ app.user.xp }} XP</li>
          </ul>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item me-2">
          <a class="btn btn-warning text-white" href="{{ path('app_admin') }}">Admin</a>
        </li>
        {% endif %}
        <li class="nav-item">
          <a class="btn btn-danger text-white" href="{{ path('app_logout') }}">Log out</a>
        </li>
        {% else %}
        <li class="nav-item me-2">
          <a class="btn btn-primary text-white" href="{{ path('register') }}">Sign up</a>
        </li>
        <li class="nav-item">
          <a class="btn btn-secondary text-white" href="{{ path('app_login') }}">Log in</a>
        </li>
        {% endif %}
      </ul>
      {# <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" />
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form> #}
    </div>
  </div>
</nav>